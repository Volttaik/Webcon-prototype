kind = "web"
previewPath = "/"
title = "Webcon App"
version = "1.0.0"
id = "artifacts/webcon-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22817

[services.development]
run = "pnpm --filter @workspace/webcon-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/webcon-app", "run", "build" ]
publicDir = "artifacts/webcon-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22817"
BASE_PATH = "/"
